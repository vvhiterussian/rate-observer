apiVersion: v1
kind: Service
metadata:
  name: {{ template "rate-observer.fullname" . }}
  labels:
    app: {{ template "rate-observer.name" . }}
    chart: {{ template "rate-observer.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.externalPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "rate-observer.name" . }}
    release: {{ .Release.Name }}
